rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Require Firebase Authentication for every document. Add tenant- and
    // role-based validation before using this database in production.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
